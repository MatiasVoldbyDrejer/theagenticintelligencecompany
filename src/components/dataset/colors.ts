/**
 * The two colours charts draw with, as literals.
 *
 * SVG fills and inline mark styles can't read a Tailwind utility, so these
 * mirror `--color-zinc-900` and `--color-accent` from the `.dataset` block in
 * globals.css. Change one, change the other.
 */
export const INK = "#26251e";
export const ACCENT = "#0087ff";
